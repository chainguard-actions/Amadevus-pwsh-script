name: ChatOps
on:
  issue_comment:
    types: [created]
permissions:
  contents: read
jobs:
  dispatch:
    runs-on: ubuntu-latest
    if: startsWith(github.event.comment.body, '/')
    steps:
      - name: /command dispatch
        uses: peter-evans/slash-command-dispatch@ace7a198016ae74cd286677c7e7f7e266eb18bc4 # v1
        with:
          token: ${{ secrets.SLASH_COMMAND_DISPATCH_TOKEN }}
          reaction-token: ${{ github.token }}
          config: >
            [
              {
                "command": "tag",
                "permission": "admin",
                "named_args": true
              },
              {
                "command": "demo",
                "permission": "read"
              }
            ]
